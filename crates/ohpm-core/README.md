# ohpm-core

Core library of **ohpm-rs**, a Rust reimplementation of the ohpm
(OpenHarmony package manager) CLI — configuration, registry client,
authentication (environment-variable driven, RSA-signature login),
publish orchestration, HAR packaging, and workspace management.

See the [workspace README](../../README.md) for the full documentation.
